# 2026-08-08

## 2026-08-08T00:00:00Z — Fix the two non-noise CodeQL findings in hermes_cli/main.py (Claude Code)

- **Agent/tool:** Claude Code (remote session), CodeQL CLI 2.26.2, `pytest`, `ruff`
- **Repos:** hermes-agent
- **Context:** A local CodeQL run (`python-security-extended`, 644 results) over
  `main` was used to settle whether PR #130 introduced alerts. It did not — those
  were line-shift attribution artifacts. But the same run surfaced two findings in
  `hermes_cli/main.py` that are *not* taint-model noise, and this change fixes both.
- **Done:**
  - **`py/tarslip` — `_install_psutil_android_compat` (`main.py`).**
    `tar.extractall(tmp_path)` with no member validation and no extraction
    filter, on a psutil sdist fetched over the network. The URL is pinned but the
    download's **checksum is never verified**, so a compromised or MITM'd sdist
    could write outside the temp dir via `../` or absolute members. Added the
    same guard `agent/curator_backup.py` already uses — reject traversal members
    up front, then `extractall(..., filter="data")` with a `TypeError` fallback
    for interpreters older than 3.11.4. (The guard was hardened further in the
    second round below — the first version checked names only.)
  - **The sibling copy.** `scripts/install_psutil_android.py:86` had the identical
    unguarded call. Its module docstring says the two copies must be kept in sync
    and removed together, so both are fixed. CodeQL flagged only the `main.py`
    one — its `py/tarslip` query needs a taint path its analysis actually
    traversed, so an equivalent bug in a standalone script went unreported.
  - **`py/incomplete-url-substring-sanitization` — `_infer_stepfun_region`.**
    `if "api.stepfun.com" in normalized` matched by substring, so
    `api.stepfun.com.example.net` inferred `china`. Now parses the host and
    compares exactly. **Impact is cosmetic** — the inferred region only
    pre-selects an entry in an interactive menu, and the endpoint actually used
    comes from `_stepfun_base_url_for_region(selected_region)` off the user's
    explicit choice. Fixed because it is two lines and strictly more correct, not
    because it was exploitable.
- **Second round, after Codex review on PR #156 — two real defects in my own fix:**
  - **The name-only guard was not equivalent to `filter="data"`.** On Python
    3.11.0–3.11.3 (in range for `requires-python = ">=3.11"`) the unfiltered
    fallback runs, and a member with a *clean name* can be a symlink whose
    **target** escapes — `psutil-7.2.2/link -> ../../outside`, then
    `psutil-7.2.2/link/pwned.txt` writes outside the destination through it.
    Reproduced with a working archive before fixing. The guard now resolves link
    targets against the link's own directory and requires containment, and
    refuses non-regular members — both of which `data` also does. Extracted to
    `agent.file_safety.assert_safe_tar_members` so there is one implementation.
  - **`urlparse(...).hostname` raises `ValueError`** on a malformed netloc
    (`https://[foo`). `_infer_stepfun_region` receives unvalidated
    `STEPFUN_BASE_URL` / `model.base_url` values, so my change turned a total
    function partial and would abort provider setup before the region picker —
    a regression against the substring version it replaced. Now catches and
    defaults to `international`.
- **Correcting a claim from this PR's first version.** It stated
  `agent/curator_backup.py` was "already correctly defended" and needed no
  change. That was wrong: it had the *identical* name-only + unfiltered-fallback
  shape and therefore the same symlink gap — and it extracts into the **skills**
  directory (executable content) rather than a temp dir, so the gap mattered
  more there. Now uses the shared helper.
- **Checked and deliberately left alone:** a grep for unguarded `extractall`
  turned up four sites. `hermes_cli/main.py:7001` is genuinely fine — an explicit
  realpath zip-slip loop over a zip, which has no symlink-member equivalent.
  `agent/curator_backup.py:619` was **wrongly** cleared in the first pass and is
  now fixed (see above).
- **Verification:**
  - New `tests/hermes_cli/test_psutil_android_tarslip.py`, **21 tests**, all
    passing — including a symlink-escape archive, an absolute link target, a
    benign internal relative link, and three malformed-netloc URLs.
  - **Positive control:** reverting only the three source files while keeping the
    tests makes **5 of them fail**; restoring the fixes returns 21/21. The tests
    detect the defects rather than passing vacuously.
  - One test initially passed for the wrong reason and was rewritten:
    `tarfile.gettarinfo(arcname="/abs/path")` silently strips the leading `/`, so
    the absolute-path case was never absolute and the guard's `startswith("/")`
    branch was never exercised. The helper now builds the `TarInfo` by hand.
  - `ruff check .` clean.
  - **Full suite via the required wrapper:** `scripts/run_tests.sh tests/hermes_cli`
    → **241 files, 5261 tests passed, 0 failed** (93.4s, 8 workers).
  - **Correcting this entry's first version.** It originally recorded a bare
    `pytest tests/hermes_cli` run and reported "5 failed / 5235 passed on clean
    `main`, 5 failed / 5250 passed with the change — same pre-existing
    ordering-dependent flakes". That verification was invalid. `AGENTS.md` says
    **"ALWAYS use `scripts/run_tests.sh` — do not call `pytest` directly"**,
    because the wrapper enforces hermetic CI parity (unset credential vars,
    `TZ=UTC`, `LANG=C.UTF-8`, per-file subprocess isolation). Under the wrapper
    there are **zero** failures — those five were an artifact of the bypass, not
    a property of the tree. Flagged by Codex review on PR #156; the conclusion
    ("not caused by this change") happened to be right, but the reasoning and
    the numbers behind it were not.
- **Third round — merged `origin/main`, and most of this change turned out to be
  superseded.** `main` had refactored the psutil sdist extraction into
  `hermes_cli/psutil_android.prepare_patched_psutil_sdist`, which does not call
  `extractall` at all: it copies file members one at a time with
  `extractfile()` + `copyfileobj()` and refuses every non-file member, so link
  members can't exist in the output. `_install_psutil_android_compat` moved to
  `hermes_cli/update_cmd.py` and calls it; the standalone script imports it too,
  which also retires the duplicated validator. Both psutil-side halves of this
  change are therefore dropped in favour of main's — main's approach is
  structurally better, since it removes the unfiltered fallback rather than
  guarding it. What survives is `assert_safe_tar_members` (still the guard for
  `curator_backup`'s fallback) and the `_infer_stepfun_region` fix.
- **A third real defect, again from Codex review — hardlinks resolve from a
  different base than symlinks.** The guard treated `issym()` and `islnk()`
  identically, resolving both against the link's own directory. That is right
  for a symlink (handed to `os.symlink` verbatim, read by the kernel relative to
  the link) and wrong for a hardlink, which `tarfile` resolves itself against
  the **extraction root** — `TarFile._extract_member` sets
  `tarinfo._link_target = os.path.join(path, tarinfo.linkname)`. So
  `a/b/link -> ../victim` under-resolved to a harmless `a/victim` and passed the
  guard, while `tarfile` linked it to `<root>/../victim`, a file beside the
  destination.
  - **Reproduced end to end before fixing**, not reasoned about: the guard
    accepted the archive, `extractall` produced a link sharing an inode with the
    outside file, and writing through it changed that file's contents. Reachable
    on Python 3.11.0–3.11.3, which is in range for `requires-python = ">=3.11"`.
  - Fixed by picking the base per link type. A test asserts the resulting
    asymmetry directly: the *same* `linkname` is refused as a hardlink and
    allowed as a symlink.
- **Test relocation.** With the psutil path gone, tests driven through
  `_install_psutil_android_compat` no longer exercise this code. The guard is now
  tested directly in `tests/agent/test_file_safety_tar_members.py`, beside the
  code and its one surviving consumer — 16 cases over member names, symlink
  targets, hardlink targets and special members, one of which *observes*
  `tarfile`'s linking behaviour rather than asserting it from documentation.
  StepFun region inference moved to `tests/hermes_cli/test_stepfun_region_inference.py`.
  Positive control re-run: reverting only the hardlink base fails exactly the two
  hardlink-escape tests.
- **Fourth round — Windows path semantics (Codex P2, real).** The guard parsed
  member names with `PurePosixPath`, but `extractall` builds its destination with
  `os.path`, which on Windows also splits on `\` and honours drive letters. A
  member named `..\outside` or `C:\outside` has no `..` part and no leading `/`
  under POSIX parsing and sailed through, while escaping at extraction. Names are
  now rejected if *either* reading says they escape, and link targets resolve with
  `\` treated as a separator. Validation is unconditional rather than
  host-switched: the archive decides whether a name is dangerous, not the machine
  reading it, and a snapshot written on one platform can be restored on another.
  The cost, recorded rather than hidden — a filename that genuinely contains a
  backslash (legal on POSIX) is now refused. 7 cases; positive control fails
  exactly those 7.
- **Disputed and not changed — "symlinked skills can no longer roll back"
  (Codex P2).** The mechanism checks out: `snapshot_skills()` uses
  `tf.add(..., recursive=True)`, which doesn't dereference, so a skill symlinked
  to an absolute path is stored as an absolute-target symlink. But the impact
  claim doesn't: `curator_backup` already extracts with `filter="data"` first, and
  `data` raises `AbsoluteLinkError` (a `TarError`, so the same recovery branch) on
  exactly that member. Verified both side by side. On Python ≥3.11.4 — everything
  CI runs — that rollback was **already failing on `main`**; the guard only makes
  3.11.0–3.11.3 behave the same way. The round-trip gap is a real bug, but it
  belongs on the snapshot side; loosening the guard to admit absolute link targets
  would reopen the escape this change exists to close, for every consumer.
  Recorded as a follow-up.
- **Fifth round — the secret-scan workflow this session added was itself broken,
  in three ways.** It surfaced by failing on this very PR.
  1. **Merging `main` made it re-scan `main`.** A merge commit's diff carries
     everything the merge brought in, so the `BEFORE..AFTER` push range scanned
     60 MB and reported 74 findings, none from the branch. Now excludes commits
     already reachable from the default branch.
  2. **A silently empty scan passed.** The first path-scoped range left a trailing
     space → empty pathspec → git error → gitleaks reported a "partial scan" of
     ~0 bytes and **exited 0**. The check went green having scanned nothing.
     Caught only because the workflow step was extracted from the YAML and run
     verbatim instead of reasoned about. All calls now go through a wrapper that
     treats `failed to scan` / `partial scan` as failure.
  3. **Merge-only content was never scanned at all.** gitleaks walks per-commit
     diffs and never attributes a merge's conflict resolution to anything —
     verified: a key introduced solely in a resolution is invisible even to a
     full-history scan, and shows up only under `--no-git`. A secret committed
     while resolving a conflict passed this check silently. The changed files'
     final content is now scanned as well.
  Also: the content pass initially used the push's before-SHA as its base, which
  on a merge push predates the merge — 7375 files, 126 findings. It measures
  against the default branch now. Verified by running the extracted step for both
  event types (12 commits / 7 files, clean) plus a planted-key control (exit 1).
- **Branch hygiene, noted not fixed.** This branch carries 78 commits from earlier
  squash-merged PRs — squashing puts new commits on `main`, so the branch's own
  history is never an ancestor of it, and every diff-scoped check reads the whole
  stale history. `git diff origin/main HEAD` is only the 7 files of this change,
  so rebuilding the branch on `main` would lose nothing; the sandbox's permission
  classifier blocks the history rewrite, so the scan is path-scoped instead. Worth
  doing properly: recreate the branch from `main` after each squash merge.
- **Test baseline.** `main` is red in this environment on its own:
  **108 failed across 42 files**, plus 4 collection errors (including a genuine
  syntax error in `tests/acp/test_session.py` that also fails `ruff` on `main`).
  The branch's failing set is compared against that baseline rather than against
  zero — see below.
- **Sixth round — the guard approach was abandoned, not patched again.** Codex
  found a *third* bypass, and it is the one that settles it: given `a -> .`, a
  directory `a/b`, and `a/b/link -> ../../outside`, the link's lexical depth is 2
  but its real depth is 1, so the target normalizes as contained while resolving
  outside. Reproduced (`file written OUTSIDE dest: True`). Containment depends on
  what earlier members *created*, which no check over member metadata can know —
  so each previous round fixed the instance and left the class.
  - `assert_safe_tar_members` is **removed**. `safe_extract_tar` uses the stdlib
    `data` filter where it exists and otherwise writes each member itself,
    creating only directories and regular files. Nothing that can redirect a
    later path is ever created, which makes the traversal question moot instead
    of answering it correctly. This is what `main` had already concluded for the
    psutil sdist in `hermes_cli/psutil_android` — the right move was to follow
    that when I first read it, rather than keep patching a validator.
  - Deliberately stricter than `data` in one respect, pinned by a test: `data`
    allows a symlink contained in the tree; the fallback refuses every link
    member. 3.11.0–3.11.3 only.
  - Tests now assert the **property** (extract for real, then check the
    filesystem) rather than that a particular check fires, with all three
    historical bypasses pinned as scenarios and both code paths exercised
    everywhere. Positive control: reinstating the unfiltered fallback fails 9.
  - **The earlier system-log rounds above describe a design that no longer
    exists.** They are left as written — the sequence is the point.
- **Seventh round — two more real defects in the content pass.**
  - **It scanned the wrong bytes for symlinks.** `[ -f ]` skipped a tracked path
    whose target dangles, and `cp` dereferenced a live one, scanning the
    *target's* content instead of the link blob git stores. Verified both. Now
    materializes `git show <rev>:<path>`.
  - **It re-reported pre-existing findings.** Editing a file that already
    contains a fixture credential failed the PR on someone else's finding —
    narrowing the file set is not the same as scanning only new content. Now
    builds a gitleaks baseline from the base revision of the same files.
  - Two details of that are load-bearing and were found only by testing: both
    scans must run from *inside* their directory (`--source .`), because a
    fingerprint embeds the file path; and both must use identical flags, because
    `--redact` rewrites the recorded `Secret`. With either wrong the baseline
    silently suppresses nothing and the check still *looks* fine.
  - Verified as a matrix over purpose-built repos: pre-existing finding → pass;
    new credential in that same file → fail; credential as a symlink target →
    fail; credential only in a merge resolution → fail via the content pass after
    the log pass reports clean.
- **Eighth round — the destination can carry the redirect, and the last fix
  broke legitimate snapshots.** Two more from Codex, both reproduced first.
  - **Pre-existing symlink in the destination.** Validating archive members is
    not enough. `curator_backup` preserves `skills/.hub` across a rollback, so an
    existing `skills/.hub/link -> /outside` makes an ordinary member
    `.hub/link/victim.txt` land outside — **with no link member in the archive at
    all**. I had narrowed "what can redirect a path" to the archive. Each path
    component is now opened with `O_NOFOLLOW` relative to its parent directory
    fd. Windows has neither `O_NOFOLLOW` nor `dir_fd`, so it keeps plain path
    operations — unchanged from before rather than a new gap, and said so in the
    code.
  - **Refusing every link member broke real backups.** `tarfile.add()` stores a
    repeated inode as `LNKTYPE`, so a skills tree with hardlinks produces a
    snapshot the previous round refused — failing on *legitimate* input, which is
    worse than the hostile-input bug it was guarding. Safe hardlinks are now
    materialized as copies; the escaping-hardlink case is still refused because
    the linkname goes through the same validation.
  - The "refuse all links" simplification looked cheap when I made it. It cost a
    working restore path, and the docstring claiming it was only stricter for
    crafted archives was wrong.
- **Ninth round — three more in the secret-scan workflow.**
  - **Quoted paths were silently skipped.** git backslash-escapes non-ASCII
    paths, so `git show "HEAD:\"r\303\251sum\303\251.txt\""` failed and the
    loop's own `|| continue` swallowed it. Now `-z` throughout.
  - **Renames.** A pure rename has no base blob under its new path → empty
    baseline → every pre-existing finding read as new. Fixed on the content side,
    which still wasn't enough: the *log* pass fired first, because path-scoping
    to the destination hides the source so git renders the rename as a wholesale
    addition. `set_pathspec` now emits both sides.
  - **One rejected.** Codex claimed the multi-file pathspec guard always fired
    because `files` contains newlines. It doesn't — grep is line-oriented, so
    `[[:space:]]` never sees the record separator, and this PR's own 9-file diff
    logged a full pathspec. Adopted the NUL parsing anyway for the quoting
    reason, and moved the whitespace test per-path so it no longer depends on
    grep's line semantics.
- **Tenth round — the baseline could suppress a real secret.** Codex flagged
  that `--baseline-path` can hide a credential that replaces a flagged fixture
  at the same location. The stated mechanism (fingerprint + redaction) was
  wrong — `Entropy` is the discriminator and survives `--redact`, so my first
  test of the scenario *caught* it. But diffing the two records showed the
  defence rests entirely on a rounded float over the secret's characters, and
  **any permutation collides exactly**:

  ```
  baseline entropy: 5.416892
  head     entropy: 5.416892   ->  no leaks found
  ```

  A different real credential, fully suppressed by a one-line shuffle.
  `--baseline-path` is replaced by `scripts/ci/diff_gitleaks_findings.py`,
  comparing on `(RuleID, Secret)`. Location is deliberately *not* in the key:
  keying on it reported every line shift and missed every in-place
  replacement — wrong in both directions. Reports are unredacted so values can
  be compared and are never echoed; a test asserts the secret reaches neither
  stdout nor stderr. 11 unit tests.
- **Reverted a fix whose premise was wrong — directory modes.** Codex reported
  that the manual path widens a `0700` directory to `0755` "unlike the filtered
  extraction path". I implemented the fix, and the test written to prove it
  failed on the **stdlib** variant: `filter="data"` also yields `0755`. So the
  hard-coded mode already *matched* the filtered path, and my change introduced
  interpreter-dependent permissions — the exact divergence this function exists
  to remove. Reverted; the test now asserts **parity between the two paths**
  rather than a mode I assumed. The underlying observation (a private skill
  directory does come back world-readable) is real but is stdlib behaviour on
  every supported version, recorded as a separate follow-up rather than fixed
  asymmetrically here.
- **Eleventh round — a hole I had explicitly rationalised away.** Codex noted
  that the `_HAVE_DIR_FD` fallback bypasses the no-follow walk entirely, so on
  Windows with 3.11.0–3.11.3 the destination-symlink fix shipped one round
  earlier did not apply at all. I had documented that branch as "unchanged from
  before rather than a new gap" and told Codex, in the previous round, that it
  was "a gap, stated rather than papered over."
  - Stating it was not closing it. Preserving prior behaviour is not the
    standard for a function whose only job is guaranteeing the property, and a
    branch that silently performs *no* enforcement is worse than one that
    admits it cannot — the caller cannot tell them apart. It now raises, naming
    the fix (upgrade to 3.11.4).
  - The change also deleted the `parent is None` branches in `_write_file` and
    `_copy_within` and tightened `_walk_dirs` to return `int`. **That optional
    return type is how the hole got in**: the signature said "this may not be
    enforced," and the code obliged.
  - Declined both suggested alternatives, with reasons: extracting into an
    empty directory relocates the traversal to the move-back step, and a
    lexical reparse-point check is Windows-only code untestable from here.
- **Twelfth round — two more, and the second was one round old and mine.**
  - **Regular-file modes were applied verbatim.** `data` sanitizes them
    (`mode &= 0o755`, clear all execute bits unless the owner had execute, then
    `|= 0o600`); the manual path used `member.mode & 0o777`, so `0777` restored
    **world-writable** on 3.11.0–3.11.3 and `0755` everywhere else. Four of six
    sampled modes disagreed. Now one shared helper, verified across **all 512**
    modes rather than the six sampled.
    - This is the *inverse* of the directory-mode report rejected last round:
      `data` ignores directory modes and does sanitize regular files. I only got
      both right by measuring each — generalizing from the first would have got
      the second wrong in the other direction.
  - **Set membership discarded multiplicity.** Switching to value-based keys the
    round before fixed in-place replacement *and* silently broke duplicate
    detection: a second copy of a credential already in the changed files was
    exempt. I tested the case I set out to fix and not the one I had just
    broken. Occurrences are counted now, with a test asserting two-at-base plus
    three-at-head reports exactly one.
  - **The recurring shape of this whole session, stated plainly:** almost every
    fix's blind spot sat immediately adjacent to the thing it fixed. Positive
    controls are what kept catching it — revert one line, confirm exactly the
    intended tests fail, and nothing else.
- **Thirteenth round — five findings, one of them leaking.** (Commit `e7b7e30`
  carries all five; its message describes only the three workflow ones, so the
  extractor changes are recorded here.)
  - **Unredacted reports outlived the step.** The JSON reports were written as
    *siblings* of the temp dirs, so the trap that removed the directories never
    touched them — while the comment beside them claimed they were trap-cleaned.
    They hold the credentials the scan finds. Verified on the sandbox: the old
    code left 603-byte reports with real findings; the fix leaves nothing.
    Everything now sits under one temp root. Also cleaned the equivalents my own
    testing had left in `/tmp`.
  - **A PR's base was overridden.** The content pass replaced a pull request's
    own base with `merge-base(default, HEAD)` whenever `EXCLUDE` was set. I had
    gated the override on a proxy for "not on the default branch" instead of on
    "this is a push". Harmless here (this PR targets `main`), wrong for any PR
    targeting a non-default branch — where a credential present only on the
    default branch would exempt the same value newly introduced into the target.
  - **Partial scans were not caught in the content pass** — the same fail-open
    the `scan()` wrapper exists to prevent, reintroduced by hand-rolling a
    second call site instead of reusing it. Both sides go through one helper now.
  - **mtimes were not restored**, so a rollback stamped "now" and changed which
    skills look recently active (`build_skill_nodes()` falls back to SKILL.md's
    mtime). Files and directories both preserved, directories after their
    children.
  - **Contained symlinks aborted the whole rollback** — the same "legitimate
    snapshot cannot be restored" failure as the hardlink case, and my own test
    was cited as the evidence, because I had written it to *document* the
    divergence rather than fix it. Now restored, with containment confirmed by
    `realpath` over the finished tree.
    - **And a claim of mine that a positive control disproved.** I wrote that
      creating links last was what made this safe. Making creation inline failed
      *nothing* — the real guarantee is that `_walk_dirs` refuses to traverse
      any symlink, ours included. Ordering is defence in depth. The docstring
      and test now say what actually holds; the overstatement would otherwise
      have shipped as documentation.
  - After this, every documented divergence from `filter="data"` is closed:
    contained symlinks, hardlinks, file modes and mtimes all match, and
    directory modes match by *not* being restored.
- **Fourteenth round — four findings, all real, all fixed in `14cfd1c`.** Three
  of the four are the same shape: a shell construct silently reinterpreting a
  value the code had already extracted correctly.
  - **`|| true` swallowed every failure the two grep patterns did not name.**
    The `scan()` wrapper caught `failed to scan` and `partial scan` — the two
    strings I had happened to observe — and passed on everything else: a kill, a
    panic, a rejected argument. That is fitting the check to the failures seen
    rather than to the class. The status is captured now and any nonzero one
    fails. `--exit-code 0` is what makes that clean: findings are explicitly not
    an error there, so no legitimate nonzero status remains to distinguish.
  - **Generated pathspecs were not marked literal.** `git diff --name-status`
    emits the tracked name verbatim, so a repo containing a file named
    `:(literal)foo` — or `:!secret.env`, or `:/anything` — handed git a pathspec
    *expression*. The named file was never walked, and an exclusion form would
    have dropped other files from the scan too. Every generated path is now
    prefixed `:(literal)`. Verified with an `exotic` fixture repo carrying such
    a name beside a credential-bearing file: clean before, exit 1 after.
  - **`$(dirname)` re-corrupted the NUL-safe paths.** Command substitution
    strips trailing newlines, so a filename ending in one lost its parent
    directory and the blob was never materialized. Proven both ways on the
    sandbox — old code materialized 1 of 2 files with the credential present in
    0 of them; new code materialized 2 of 2 with it present in 2. Now
    `${dst%/*}`. **This is the second time a shell idiom has undone the
    NUL-safety in this loop** (the first was the pathspec quoting in round
    nine), which is a decent argument that the materialization belongs in the
    Python helper rather than in bash. Not done here — the change is already
    large — but recorded as the structural answer rather than patching a third
    occurrence later.
  - **Hardlink copies lost their mtime.** Round thirteen restored mtimes for
    regular files and directories and missed the hardlink path, which writes
    through `_copy_within`. Now threaded, covered by
    `test_a_hardlinked_sibling_keeps_its_mtime`. The miss surfaced as a real
    divergence only because the `extract` fixture is parameterized over
    `stdlib-filter` and `manual-fallback` — the fallback is held to what
    `filter="data"` actually produces, not to my reading of it.
  - Commit `3f71d2b` then removed a comment block the review diff showed had
    been duplicated by an earlier edit. Full workflow matrix re-run after all
    fixes: this branch exits 0; `bt2`, `uz`, `glrepo`, `exotic` all exit 1
    correctly. 89 tests across the three affected files, `ruff check .` clean.
- **Fifteenth round — two findings, and the first one caught a false claim I had
  published twice.** (Commit `818ed07`.)
  - **A literal backslash in a member name was silently relocated, not
    refused.** `_safe_member_parts` rewrote every `\` to `/` before splitting,
    and both the docstring and the PR body described that as *refusing* a
    backslash filename. It was nothing of the kind: `demo/a\b` became
    `demo/a/b`, so a snapshot holding both entries restored only one and a
    rollback lost a file without reporting anything. The stdlib disagrees —
    `filter="data"` keeps the two distinct on POSIX — which is what makes this a
    divergence rather than a design choice. Validation and splitting are now
    separate concerns: validation still runs under both POSIX and Windows
    readings so the Windows escapes stay refused everywhere, while splitting
    uses only the host's semantics. Three tests; before the fix exactly the two
    positive ones failed and only under `manual-fallback`.
  - **Windows on 3.11.0–3.11.3 cannot roll back at all** — `_HAVE_DIR_FD` is
    false there, so `_walk_dirs` refuses before the first entry, while
    `requires-python` still advertises the configuration. My earlier docstring
    framed that as an acceptable trade, which quietly turned "works, unsafely"
    into "always fails". **Attempted and reverted:** flooring
    `requires-python` at `3.11.4` is the right fix and a no-op for every real
    install path, but it is written into `uv.lock`, and this environment's `uv`
    cannot parse the committed lockfile's `[options.exclude-newer-package]`
    schema — so the lock cannot be regenerated and `uv lock --check` would fail
    CI. Shipping that is worse than the gap, so it is a follow-up. The suggested
    alternatives were declined on the record: staging-and-swap moves the problem
    to the swap (rollback preserves `skills/.hub`, so the destination is
    populated), and reparse-point enforcement would be TOCTOU-racy security code
    for a platform this sandbox cannot execute — in a module where every
    hand-rolled containment scheme so far has been bypassed.
  - **A verification claim of mine was also wrong.** I reported "`ruff check .`
    clean" earlier in the session. `uv run ruff check .` exits 2 here — the same
    `uv.lock` parse failure, before ruff runs. Invoking ruff directly with
    `--config pyproject.toml` gives the real result (`All checks passed!` on the
    full tree). Pre-existing, confirmed identical on a stashed tree, but the
    claim rested on an exit code I had not checked.
- **Sixteenth round — two findings, both confirmed by construction.** (Commit
  `342a501`.)
  - **Fractional mtimes were truncated at all three write sites.**
    `member.mtime` is a `float` for PAX archives and `filter="data"` restores
    the fraction; the fallback coerced it with `int()` for regular files,
    directories and hardlink copies alike. So a rollback's timestamps depended
    on the interpreter, and two skills touched within the same second collapsed
    to the same time — which is the ordering `build_skill_nodes()` reads, so
    this was behaviour, not just metadata. Three parametrized cases; restoring
    `int()` fails exactly those three and only under `manual-fallback`.
  - **The content scan baselined on the merge base, which squash history makes
    stale.** Squashing puts a *new* commit on the target, so a branch's own
    commits never become ancestors of it and the merge base stays at the
    original divergence point. Everything already merged then reads as changed,
    is materialized against a baseline predating it, and its accepted findings
    are re-reported. Built the case to confirm: a fixture repo whose credential
    file was squash-merged into `main` reports `fixture.py` as *introducing*
    that credential (exit 1) under the old baseline and exits 0 under the new
    one. `CONTENT_BASE` is now the target branch's **tip** — `origin/BASE_REF`
    on a PR (so the round-thirteen non-default-target fix is intact),
    `origin/DEFAULT_BRANCH` on a push. The range scan still uses the merge base,
    correct there. **This retires a caveat I had written into the file as
    unfixable** — I recorded the squash noise as "the real fix is branch
    hygiene" and path-scoped around it, but path scoping was treating the
    symptom and the baseline was simply the wrong commit. The content pass now
    scopes to 11 files on this PR, matching its actual changed-file count.
  - Matrix unaffected: `bt2`, `uz`, `glrepo`, `exotic` all still exit 1; this
    branch exits 0 on both event types. 114 tests, 0 failed.
- **Seventeenth round — deleted files were missing from the content baseline.**
  (Commit `97928d8`.) Same shape as the rename fix in round nine: I handled the
  pair `-M` reports and missed the case it does not see. A merge resolution that
  relocates a *block* out of a file being deleted produces `D` + `M`, not a
  rename, and the `D` branch discarded the path without materializing anything.
  Because the comparison is a multiset over `(rule, secret)`, the moved value
  had a base count of zero and its HEAD occurrence read as introduced — though
  it had been in the base tree all along. Deletions are now materialized on the
  base side only; `COUNT` stays un-incremented, since it gates on there being
  HEAD content worth scanning. Built the case: credential in `old.py` on `main`,
  resurfacing in `keep.py` only via a conflict resolution, `old.py` deleted →
  old code "0 already present at the base, 1 new" (exit 1), new code "1 already
  present, 0 new" (exit 0). The range scan is silent in both, which is the point
  — it never attributes merge content, so the content pass alone decides.
  Discriminating rather than merely quieter: the same fixture with a value *not*
  on `main` still exits 1.
- **Full-suite comparison re-run on the round-17 tree.** The earlier figure
  predated rounds 14–16, so it was measured again rather than carried forward:
  `origin/main` 48 failing files, this branch 47, **zero branch-unique**, one
  fixed (`tests/acp/test_session.py`); 25,722 passing. The red CI slices were
  also checked against the baseline directly instead of assumed — slice 4/8's
  four failing files all fail on `main`, and its visible cause (`NameError:
  name '_warn_if_gateway_lifecycle' is not defined` at `hermes_cli/cron.py:407`)
  is in a file this branch does not touch.
- **Eighteenth round — a regression I introduced in round sixteen, defended by a
  comment that was wrong.** (Commit `1f42790`.) Before the round-sixteen
  rewrite, a push fell through to `CONTENT_BASE="$BASE"` — the before-SHA — so
  the content pass ran on the default branch. The rewrite gated it on `EXCLUDE`,
  which is empty on the default branch by construction, and the comment I wrote
  in its place asserted the skip was "same as before". It was not. Merge-
  resolution coverage silently disappeared from the one branch that ships.
  Demonstrated rather than argued: on a repo where a side branch is merged into
  `main` with the credential only in the resolution, `git log -p BEFORE..HEAD`
  matches it **zero** times (the range scan is structurally blind), and the step
  exits 0 under round-seventeen code and 1 after the fix. The default branch now
  uses the before-SHA, which is also the only workable baseline there — the tip
  is where we already are, so it would name no changed files, and the stale-
  divergence problem cannot arise because on that branch the before-SHA *is* the
  previous tip. Feature-branch pushes with no reachable default branch still
  skip, deliberately.
  - **The pattern, now four instances.** Every defect in this file has been a
    check passing on an input it never examined: the empty pathspec, the
    `partial scan`, the dropped blob, the swallowed nonzero status — and now an
    entire branch of the input space routed around the check. The first four
    were caught by *running* the step against a fixture; this one survived
    because I reasoned about control flow in a comment instead of executing that
    path. The fixture is twenty lines. Write the fixture.
- **Nineteenth round — the exact inverse of round sixteen, which is what made it
  useful.** (Commit `9d6bb63`.) Codex reported that a feature branch merely
  *behind* the target differs from it wherever the target moved on, so if the
  target scrubbed a flagged value after divergence, this branch still has it,
  the diff names that file, and it reports as introduced by a push that never
  touched it. True — and flipping back to the merge base would simply have
  reinstated round sixteen. **I had been oscillating between two false
  positives**: merge base alone re-reports squash-merged content, target tip
  alone re-reports target-only changes.
  - The resolution is that these are two questions and one commit cannot answer
    both. `DIFF_BASE` (merge base) selects *which files* — what this branch
    changed, not what the target changed underneath it. `CONTENT_BASE` (target
    tip) supplies the *exemption baseline* — a value the target already ships,
    however it got there, is not reported again. On the default branch the two
    collapse to the before-SHA, so round eighteen is preserved.
  - Verified on a behind-main fixture (`main` scrubs `legacy.py`, feature
    touches only `app.py`): round-eighteen code names 2 files and reports
    `legacy.py` (exit 1); fixed code names 1 file and exits 0.
  - **The squash case now passes for a better reason.** In round sixteen it
    passed by being excluded from the file set; now it is in the set and
    exempted by the baseline ("1 finding, 1 already present at the base"). That
    is the right shape — "already on the target" is a claim about content, and
    it should be settled by comparing content, not by never looking at the file.
  - Six fixture classes now pinned: `bt2`/`uz`/`glrepo`/`exotic`, squash-history,
    deleted-source move (both polarities), default-branch merge push, and
    behind-main.
- **Twentieth round — the round-seventeen fix crashed on a file-to-directory
  refactor.** (Commit `63a5d17`.) Putting deleted blobs into `BASE_DIR` ignored
  that a file and a directory cannot share a name. `config.py` →
  `config.py/__init__.py` is reported by git as `D config.py` + `A
  config.py/__init__.py`; the deleted blob lands at `BASE_DIR/config.py`, the
  later `mkdir -p BASE_DIR/config.py` fails, and `set -e` aborts the job on a
  change with no secret in it — `mkdir: cannot create directory ... File
  exists`, exit 1.
  - Deletions now get their own tree. Two properties make that right rather
    than merely different: it **cannot collide with itself**, since one commit
    cannot have a path as both a file and a directory; and the relative layout
    is **preserved rather than flattened**, because `.gitleaks.toml` has
    path-scoped allowlist entries and flattening would apply them to one side
    only — silently asymmetric exemption, worse than a crash that announces
    itself.
  - **Ordering deletions last was considered and rejected.** It converts the
    crash into the reverse collision: the redirect lands on a directory, `||
    true` swallows it, and the base blob is silently dropped. Trading a loud
    failure for a quiet false positive is the fail-open pattern this file has
    spent five rounds eliminating.
  - `diff_gitleaks_findings.py` gains a repeatable `--extra-base`; deleted
    findings remain base-side findings in the same multiset. Six tests, one of
    them a control asserting the value *is* new without the extra report — so
    the test proves the report does the work, not the key.
  - Seven fixture classes now pinned. Round seventeen's exemption still lands,
    via the separate tree; that was the case most at risk from this change.
- **Twenty-first round — two findings, both "a constant that was really a
  measurement".** (Commit `7e45788`.)
  - **Fallback directory mode hard-coded `0755`.** `filter="data"` clears the
    archived mode and lets `os.mkdir`'s default 0777 meet the process umask.
    `0777 & ~022` is `0755`, so the hard-coded value coincided with the right
    answer under the one umask I had tested. Measured across four: stdlib gives
    0755/0775/0777/0700 under umask 022/002/000/077, the fallback gave 0755
    everywhere — so a group-shared skills tree lost group write depending on the
    interpreter's patch level. **Round twelve's conclusion was right and its
    generalisation was wrong**: I correctly reverted mode *preservation* after
    my own test failed on the stdlib variant, then wrote the observed constant
    into both the code and the test. The test now computes `0o777 & ~umask` and
    runs under four umasks; before the fix exactly the 002 and 000 cases fail,
    and only on `manual-fallback`.
  - **Renames baselined from the source against the wrong revision.** Fallout
    from the round-nineteen two-base split: `src` comes from rename detection
    against `DIFF_BASE`, but the lookup reads `CONTENT_BASE`, and those became
    different revisions in that change. A rename already squash-merged is still
    reported `R old new` against the merge base while the target has only
    `new` — so the lookup found nothing, left an empty baseline, and reported a
    credential already on `main` as introduced by a push that only touched
    `app.py`. Destination first now, source as fallback. The control that
    matters is the other direction, and it holds: a fresh rename the target has
    not seen still resolves through `src`, so round nine does not regress.
  - Nine fixture classes now pinned. Both fixes are the same move — derive the
    value at the point of use instead of freezing one observation of it.
- **Twenty-second round — a P1 that destroyed data outside the tree.** (Commit
  `18f1878`.)
  - **An existing hardlink at a destination leaf was truncated in place.**
    `O_NOFOLLOW` rejects a *symlink* there and says nothing about a hardlink,
    because a hardlink is not a reference to the file — it *is* the file. Since
    `curator_backup` preserves `skills/.hub`, a hardlink sitting there and
    pointing outside the tree had that outside file overwritten. Reproduced on
    **both** paths: outside file reading `PWNED` at `nlink=2`, which is what
    makes it unambiguous — same inode, rewritten through the preserved name.
  - Fallback: unlink then create with `O_EXCL`. Stdlib path: it has the same
    hole and is the one that actually runs, so a pre-pass detaches hazards —
    any leaf that is a symlink or a regular file with `nlink > 1` — before
    handing over. **The pre-pass is deliberately best-effort.** Reusing
    `_walk_dirs` was the first instinct and would have been wrong: it refuses
    to traverse a symlinked directory, which is right for the fallback but
    would newly break the stdlib path for *legitimate* symlinked skills, which
    `agent/skill_utils.py` supports. It skips what it cannot resolve instead of
    raising. Symlink-at-the-leaf was swept in as the same class one level down.
  - **Branch-wide file set re-reported the branch's own earlier findings on
    every push.** The range scan walks `BEFORE..AFTER` and reports a commit
    once; the content pass did not. Fixed *without* narrowing `DIFF_BASE` —
    doing that would reintroduce round thirteen (a merge push names everything
    the merge carried, 7375 files, now exempt but still materialized, risking
    the 10-minute timeout). The previous branch head becomes an extra exemption
    base instead, reusing round twenty's `--extra-base`. Verified in all three
    directions, and the third is what makes it safe rather than merely quieter:
    the introducing push still fails, a later unrelated push goes green, and
    **the PR event keeps the branch-wide view**, so the branch still cannot
    merge green. Push noise goes; the gate that matters does not move.
  - Eleven fixture classes now pinned.
- **Twenty-third round — four findings, three of them holes in the previous
  round's fixes.** (Commit `d6ce18c`.)
  - **Normalization mismatch (P1).** The round-22 pre-pass used
    `_safe_member_parts`, which rejects an absolute name, while `filter="data"`
    strips the leading slash and extracts anyway. `/.hub/x` was therefore
    skipped as "extraction will reject this" and then extracted, straight
    through the preserved hardlink — outside file `PWNED`, `st_nlink` still 2.
    The pre-pass now mirrors the filter's own normalization.
  - **Detaching before validation (P2), and my first answer was wrong.**
    Cancelling the pass when a member looks unacceptable — the obvious fix —
    measured *worse*: members extract in order, so an appended invalid member
    disables the pass and the hardlink is truncated anyway. Raising up front is
    the only option that leaves the destination *and* the preserved state
    alone, and it is safe because both checks are no stricter than `data`. Link
    targets are deliberately unchecked, since `data` allows a contained
    `../sibling` and refusing those would break round eight's symlinked skills.
  - **Overlapping baselines (P1)** — the round-22 extra base reintroduced the
    round-12 multiplicity bug in a new place. `base.json` and `prev.json` are
    *alternative* snapshots of the same paths, not disjoint slices, so summing
    them exempted two copies of a value each parent holds once. New
    `--alt-base` combines by maximum; `--extra-base` still sums, correctly,
    because the deleted-file tree really is a disjoint slice. A control test
    asserts the old flag on the same inputs still exits 0.
  - **`//` anywhere is not an authority (P2).** A bare host with a doubled
    slash in its path was left unprefixed, parsed entirely as a path, and gave
    an empty hostname — reporting a live China endpoint as international and
    offering to rewrite it to the `.ai` host. Now tests for a scheme or a
    leading `//`.
  - **The common thread, worth naming:** each was a *new mechanism* bolted onto
    a checked one without re-deriving what the existing invariant required of
    it. The pre-pass reused a stricter parser than the thing it was guarding;
    the extra baseline reused a summing merge for snapshots that overlap.
- **Twenty-fourth round — five findings, four of them P1.** (Commit `ad86f38`.)
  - **No `dir_fd`, no detachment — and not only on the old interpreter.** The
    filtered path reaches the same pre-pass, so returning early left the
    hardlink hole open on 3.11.4+ too; upgrading Python did nothing. Refusing
    would disable rollback on Windows (the round-18 trade, declined again), so
    the detach falls back to a path walk that `lstat`s each component before
    descending. **Racy where the `dir_fd` route is not, and the docstring says
    so** rather than implying parity. Unlike round 18's reparse-point idea this
    one is testable on Linux by forcing the flag, which is why I was willing to
    write it.
  - **Hardlink sources were taken from the filesystem.** `linkname` was resolved
    against the extraction root and whatever sat there was copied — so a crafted
    snapshot could name a *preserved* file no member created (`.hub/secret.txt`)
    and pull it into the restored tree; the stdlib path made a real hardlink, so
    later writes mutate state rollback excludes. The source must now be an
    earlier member. Free of risk: `tarfile.add()` only emits `LNKTYPE` for an
    inode it has already archived.
  - **Out-of-range timestamps aborted recovery.** `os.utime` raises
    `OverflowError`, which is not a `TarError`, so `rollback()` skipped its
    extraction-failure recovery — and the *stdlib* path does the same, so this
    was never fallback-only. Rejected before extraction now. **The test's
    assertion had to change**: "extraction succeeds" is unachievable on the
    filtered path, and the property that matters is the exception *type*. `NaN`
    was dropped from the cases — `tarfile` cannot write one, so testing it would
    assert a fiction.
  - **Maximum drops disjoint occurrences** — the third position on this
    question, after summing was wrong the other way in round 23. Both simple
    merges discard the only thing distinguishing occurrences *between* parents:
    location. Reports are now unioned over `(rule, secret, file, line)`, per
    report counted then maxed across, so two findings at one location in one
    report still count twice. Location remains outside the head-vs-base key, and
    that distinction is now in the module docstring because it reads as a
    contradiction otherwise.
  - **New branches were scanned one commit deep.** An all-zero before-SHA can
    accompany many commits; falling to `HEAD~1` missed a credential added
    earlier in the same push. **My first version of this fix broke two other
    fixtures** — widening the base without the exclusion and path scoping every
    other range scan uses reinstated round 16. Also fixed a fault in my own
    harness: two fixtures fed an all-zero before-SHA to branches with real
    history, so they were exercising the new-branch path instead of the one they
    were written for.
  - Twelve fixture classes now pinned.
- **Twenty-fifth round — three P1s, two of them in round twenty-four's work.**
  (Commit `73054c1`.)
  - **A symlink member redirects a later member past the pre-pass** — round
    six's bypass reappearing inside the thing that replaced round six's guard.
    `a -> .hub` then `a/x`: nothing named `a` exists when the pass looks, and
    `extractall` then creates the link and writes through it onto the preserved
    hardlink. I had written the lesson down in round six — *a pass over member
    metadata cannot know where a member lands* — and did not carry it across.
    Members under a symlink member are now refused; decidable from members
    alone, and safe because tar never archives content beneath a symlink.
  - **Junctions are not `islink()`.** The round-24 walk used
    `os.path.islink()`/`isdir()`; a Windows directory junction is False for the
    first and followed by the second, so the walk descended through one and
    **unlinked an entry outside the tree** — the replacement doing damage the
    early return had merely permitted. Now `lstat`-based, rejecting any reparse
    tag, and stopping outright on Windows if the attribute is absent.
    - **The pattern, stated because it is mine.** In round eighteen I declined
      reparse-point work as "security-critical code for a platform this sandbox
      cannot execute". In round twenty-four I wrote a path walk anyway and
      called it testable. What I could test on Linux was the symlink case; the
      Windows semantics that broke it are exactly what Linux cannot exercise.
      The justification was true of the code I ran and false of the code I
      shipped.
  - **Line shifts broke parent identity.** Reconciling on `(rule, secret, file,
    line)` looked more precise and was less correct — an inherited credential
    routinely sits at different lines in the two parents, and counting those as
    two occurrences inflated the exemption enough for a merge resolution to slip
    a new copy past. Per file now.
  - **Recommendation recorded on the PR rather than another patch.** Three of
    the last six findings were pre-pass defects, and the first of today's shows
    why: the pre-pass is asked to predict where members land, which round six
    proved cannot be done from member metadata. Each round closes the instance
    and the class survives. The structural answer — extract into a fresh empty
    staging tree and swap — has been a listed follow-up since round twenty-two;
    with an empty destination the whole class disappears rather than being
    guarded. It belongs in `curator_backup`, and it wants its own change.
- **Twenty-sixth round — two more P1s in the pre-pass, so the pre-pass is
  gone.** (Commit `3dc272a`.) Six consecutive rounds produced a P1 there. Last
  round I concluded the approach was structurally wrong and recommended
  deferring the fix; this round I acted on the conclusion instead of patching a
  seventh time.
  - **The structural fix was far cheaper than I had estimated**, and I only
    found that by reading `rollback()` properly. It *already* moves every
    top-level entry aside except `.hub` and `.curator_backups` — its own comment
    says "so the extract happens into an empty skills tree" — and
    `snapshot_skills()` excludes those same two names. So they are the only
    things extraction can collide with, and no legitimate archive contains a
    member under either. **Refusing such members means extraction only writes
    paths that do not exist yet**, and an empty destination has no hardlink,
    junction or symlink to abuse.
  - `_detach_leaf` and the detachment loop are **deleted**, including the
    unverifiable Windows reparse-point handling. What remains is
    `_validate_members`, deciding everything from the members alone — the
    boundary round six actually established.
  - **Hardlink sources must be an earlier *regular file* member.** "Earlier
    member" was not enough: `a -> .hub/x` (symlink), `b -> a` (hardlink), then a
    regular `b` made `b` an alias the final member followed. Safe to tighten:
    `tarfile.add()` only emits `LNKTYPE` for a regular inode it already
    archived.
  - The round's second finding — a contained symlink already in `.hub` — needed
    no rule of its own; it requires writing under `.hub`, so the exclusion
    covers it. All eight redirects found across rounds 22–26 now refuse on both
    paths. Removing the one rule fails nine tests; the control confirms an
    ordinary archive still extracts into a live destination.
  - **On last round's recommendation:** right about the problem, wrong about the
    cost. I estimated a rollback redesign without reading `rollback()` closely
    enough to see it already staged the tree aside. Worth remembering before
    deferring the next thing as "too big".
- **Twenty-seventh round — two fixed, one declined with evidence.** (Commits
  `7e52df9` and the docstring follow-up.)
  - **The preserved-name refusal was bypassable by spelling.** `.hub\x` is one
    opaque component to `PurePosixPath`, so last round's boundary saw no
    `.hub` — while `tarfile` builds its destination with `os.path` and treats
    the backslash as a separator on Windows. The first component is now taken
    under both readings. Same lesson as round twenty-one (*validate under both
    flavours, act under the host's*), which I had applied there and not carried
    to the new check.
  - **Directory members skipped the timestamp range check.** `data` applies
    directory attributes *after* the members, so an out-of-range mtime on a
    directory escaped as `OverflowError` from the very end of `extractall` —
    past everything else, and not a `TarError`, so `rollback()` skipped
    recovery. Validated before the short-circuit now.
  - **The third finding is unsatisfiable alongside round twenty-five's, and is
    declined.** Both present parents holding one occurrence each and a HEAD
    holding two; they differ *only* in whether HEAD's lines coincide with the
    parents'. Tested the rule that separates them — exempt a head finding when
    some parent has the same `(file, line)` — and it satisfies rounds 23–27
    while failing **round twelve**, the moved secret, which is the reason the
    comparison is content-keyed at all. So the choice is between over-exempting
    (a merge resolution slips a real credential past) and under-exempting (an
    unrelated merge push fails on a credential both parents had). Kept the
    reporting direction, since a false failure costs one glance and a miss is
    what the pass exists to prevent. **Written into the module docstring** so
    the next reader inherits the reasoning rather than rediscovering the
    oscillation.
- **Twenty-eighth round — two findings, both fixed.** (Commit `9ea2869`.)
  - **Directory members skipped every check**, so a `DIRTYPE` member named
    `.hub/injected` was created inside the tree rollback exists to leave alone,
    with rollback reporting success. **The second directory short-circuit bug in
    two rounds** — the first was timestamps — and both had the same shape: a
    `continue` placed ahead of checks that were never type-specific. Validation
    that applies to every member now runs before it. Also worth recording *why*
    no existing test caught it: every redirect test asserts the outside file is
    untouched, and nothing is written *through* a bare directory. The violated
    property was different — rollback must not mutate hub state at all.
  - **A duplicate member name invalidated provenance.** Regular `a`, then a
    symlink also named `a`, then hardlink `b -> a`: `a` was a regular file when
    the hardlink was validated and a symlink when extraction used it, defeating
    round twenty-seven's source-type rule. Fixed by refusing duplicate
    destinations rather than invalidating provenance on replacement — the
    latter leaves the general problem, since *any* conclusion about a path can
    be undone by a later member with the same name. `tarfile.add()` walks a tree
    once, and `test_a_real_archive_of_repo_content` would fail if the rule were
    too strict for ordinary archives. Round twenty-six's move again: prefer the
    rule that removes the possibility over the one that tracks it.
  - The validator is now, in full: member type, path safety under both
    separator readings, preserved-name refusal, representable timestamps, no
    duplicate destinations, no member beneath a symlink member, hardlink sources
    that must be an earlier regular-file member. All decided from the members
    alone, before anything is written.
- **Final full-suite comparison against the same-machine `origin/main` baseline.**
  Both sides run under `scripts/run_tests.sh` (per-file subprocess isolation), so
  the comparable unit is the set of *files* containing a failure or a collection
  error, not the raw count. `main`: 48 such files. This branch: 47. **Zero files
  fail on the branch that do not also fail on `main`** — no regressions. The one
  difference is in the branch's favour: `tests/acp/test_session.py`, whose
  unclosed list literal was a real syntax error on `main` (it had silently killed
  15 tests and also failed `ruff` there). The Python CI slices are red for the
  same pre-existing set; the JS/TS ones for the react/react-dom mismatch
  diagnosed below. Neither is caused by this change.
- **The JS/TS suites are red on `main` for a dependency reason — diagnosed, not
  fixed.** I had been asserting these were pre-existing on the causal grounds
  that this branch changes no JS, TS, JSON, manifests or lockfiles. The actual
  cause is better evidence:

  ```
  Error: Incompatible React versions: react and react-dom must have the exact
  same version.  react: 19.2.8   react-dom: 19.2.7
  ```

  Every workspace `package.json` declares `19.2.7`, but the **root hoisted**
  `node_modules/react` resolves to `19.2.8` while `react-dom` stays at `19.2.7`
  — and `package-lock.json` is byte-identical on `origin/main`, so a root bump
  landed without `react-dom` following. It fails four suites in `web/` alone
  (147 tests still pass; the four failures are all this import-time error).

  Deliberately **not** fixed here, unlike the two one-liners above. Realigning
  needs a regenerated lockfile via `npm install`, which is neither verifiable in
  this sandbox nor a one-line judgment call — whether to pull root react back to
  19.2.7 or push react-dom to 19.2.8 is a dependency-policy decision, and the
  repo has dependabot for exactly this. Worth raising separately, since it
  blocks every JS check repo-wide.
- **Blocked on a human, correctly.** `Review label gate` requires the
  `ci-reviewed` label because this PR touches `.github/workflows/secret-scan.yml`.
  Not self-applied — that gate exists so a person reviews CI changes, and the
  workflow edits here are the part most worth reviewing.
- **Commits/PRs:** branch `claude/daily-repo-scan-v8fvqs`
- **Follow-up:**
  - **The psutil sdist download has no checksum.** Pinning a sha256 would close
    the gap the tarslip guard only contains, and would match the repo's own
    Dependency Pinning Policy and the pinned-and-checksummed gitleaks download in
    `secret-scan.yml`. Not done here: the only hash available to compute is
    whatever the server serves right now, so it is trust-on-first-use rather than
    a verified pin. Worth doing deliberately.
  - **CodeQL is advisory, not required.** Auto-merge merged #130 over a red
    CodeQL check. Making it a required status check is a repo-settings change.
  - **`main.py` carries 13 open `py/clear-text-logging-sensitive-data` alerts**
    (severity 7.5), untouched here — mostly model names and truncated key
    prefixes. Repo-wide the count is 333, which is the volume problem
    `supply-chain-audit.yml`'s own header warns about: a scanner that always
    fires trains reviewers to ignore it.
