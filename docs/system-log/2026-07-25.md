# 2026-07-25

## 2026-07-25T05:20:00Z — Mnemosyne evaluated as a memory provider (Claude Code, remote session)

- **Agent/tool:** Claude Code (remote session, branch `claude/mnemosyne-evaluation-09bk1s`)
- **Repos:** hermes-agent (docs only); companion entry in crm-pipeline
  `docs/system-log/2026-07-25.md` — same upstream evaluated as a KB semantic-index
  backend, also rejected there
- **Done:** New `docs/mnemosyne.md` evaluating
  [`mnemosyne-oss/mnemosyne`](https://github.com/mnemosyne-oss/mnemosyne) @ `main`
  (v3.15.0, `mnemosyne-hermes` 0.5.0) as a tenth external memory provider.
  Verdict: **opt-in plugin, pinned — not a default.** In its favour: 2285 tests
  pass locally at CI parity in 5m23s; local-first is real (traced
  `core/embeddings.py::_is_api_model()`); SHA-pinned Actions; MCP SSE refuses
  non-loopback without a token; 326 commits/90d with a two-day fix turnaround on
  #491; and the provider implements all four `MemoryProvider` abstract methods
  plus most optional hooks. Against it: their CI sets `MNEMOSYNE_NO_EMBEDDINGS=1`
  so the vector path is uncovered; degradation to lexical is silent (#474, #518,
  reproduced locally at 1/3 recall on trivial queries); `ALL_TOOL_SCHEMAS` exposes
  40 tools / ~6.5k tokens per request against a README claiming 23, and
  `mnemosyne_triple_end` is advertised over MCP with no `_TOOL_HANDLERS` entry
  (raises `ValueError`); two divergent in-tree copies of the provider
  (`hermes_memory_provider/` 3778 lines vs `integrations/hermes/.../mnemosyne_hermes/`
  2882) reached by different install routes; #487 (gateway RSS jump with
  `mnemosyne-hermes` 0.4.0) and #537 (`hermes update` drops the provider) land on
  us directly; open correctness bugs #507/#506/#524/#525/#523; #482 with 50 of 106
  config keys silently ignored; and their own 2026-06-09 audit found 71 doc
  defects including 7 of 9 fictional env vars. Governance noted: CLA pre-authorises
  relicensing under "any future open-source or commercial license", bus factor ≈ 2
  (~68% of 90-day commits from two authors), and `SECURITY.md` points at the retired
  `AxDSan/mnemosyne` tracker while instructing reporters to file a "confidential"
  GitHub issue — a feature GitHub does not have, so following it publishes the vuln.
  Pilot conditions listed in the doc (pin the version, assert `vec_episodes` at
  `initialize()`, trim the tool set, watch RSS, configure by env var not `config.yaml`).
- **Commits/PRs:** this branch's PR.
- **Follow-ups:** If piloted, add a provider plugin under `plugins/memory/mnemosyne/`
  and extend `website/docs/user-guide/features/memory-providers.md` (currently
  documents 9 providers). Worth reporting the ghost `mnemosyne_triple_end` MCP
  schema and the `SECURITY.md` disclosure-channel error upstream.
