
## 2026-08-12T16:05:00Z — Repair the Python suite (Claude Code)

**Repos touched:** `dizhaky/hermes-agent`

### Why this exists

Making the aggregate gate real (#186) surfaced that `main`'s Python suite is
red — six of eight CI slices. A gate that blocks on a red suite blocks
everything, so #186 is held as a draft until this is green.

### Correction to my own sizing

I first reported "69 failing tests across 24 files" from a local full-suite
run. **That number is this sandbox, not the repo.** The venv is missing
optional packages (`PIL`, `anthropic`, `hindsight_client_api`) and carries a
different `aiohttp` than `uv.lock`, which accounts for most of it — 34 of those
failures are `FeatureUnavailable: lazy installs disabled` or
`ModuleNotFoundError` for optional extras.

Some of them would be actively *wrong* to "fix":
`test_every_lazy_deps_exact_pin_matches_uv_lock` fails here with
`aiohttp=['3.14.1'], expected ['3.14.3']` — it is correctly detecting drift in
my environment, and editing the pin to match would corrupt the real one.

The environments genuinely disagree in both directions:
`tests/gateway/test_gateway_command_line_matcher.py` fails in CI (9 tests) and
passes locally. So CI is the oracle for what blocks the gate, and this branch
exists partly to make CI enumerate it.

### Fixed so far — two real bugs, both environment-independent

**`plugins/memory/mem0/__init__.py` — `NameError: name 'user_id'`** (3 tests,
CI-confirmed). `_start_prefetch(self, query)` computed
`effective_user_id = user_id or self._user_id`, but `user_id` is not one of its
parameters. It is a copy of the identical line in `sync_turn` fifty lines
below, where `user_id` genuinely *is* a parameter. The computed value was never
read — the closure filters via `self._read_filters()`. Deleted the line rather
than adding a parameter: nothing wanted the value.

**`hermes_cli/gateway.py` — `NameError: name 'prog_args'`** (3 tests). The
launchd plist builder assembled `core_args`, computed a `use_wrapper` flag, and
then called `prog_args.append(...)` — a name that is never assigned. All three
of `core_args`, `use_wrapper` and `prog_args` were inconsistent, so the function
could not ever have run to completion.

The surrounding comment states the intent precisely ("the wrapper execs the
python + module args we pass after it… if the wrapper is missing, fall back to
the direct-python form"), and the three failing tests assert nothing about
wrapper shape, so the comment is the spec: `prog_args` is the wrapper path in
front of `core_args` when the wrapper is usable, and `core_args` alone
otherwise.

### Not fixed — needs evidence, not a guess

`tests/hermes_cli/test_config_env_expansion.py::TestExpandValueFromEnviron` (5)
and `tests/tools/test_approval.py` (4) do function-level imports of
`_expand_value_from_environ` and `_redact_command_for_display`, neither of which
exists. Implementing them means inventing semantics — the config one would have
to expand `${VAR}`, honour `${VAR:-default}`, expand bare `$VAR`, and yet leave
`literal-$var` untouched — in a security-adjacent config loader. Whether those
helpers were deliberately removed or never written is not answerable from a
shallow clone, so they wait for CI to confirm they even fail there.
