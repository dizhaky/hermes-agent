# 2026-08-25

## 2026-08-25 — Point blocked ollama-url cron jobs at named litellm_proxy

**Repos touched:** `dizhaky/hermes-agent`

`scripts/fix-cron-ollama-base-url.py` no longer hardcodes `custom` + `http://localhost:4000/v1`. On mfc1 that port is closed; LiteLLM listens on Tailscale `:4010` and requires `LITELLM_MASTER_KEY`. The script now reads `custom_providers.litellm_proxy` from Hermes config (named provider + configured host so the safety-gate host-match and stored key both work) and falls back to the old localhost pair only when that provider is absent.

Live apply (mfc1 `~/.hermes/cron/jobs.json`) is the remaining operator step after merge; five target job IDs are unchanged.

## 2026-08-25T14:57:00Z — Prefer v12 litellm_proxy and restrict cron auto-resume (Cursor)

- **Agent/tool:** Cursor
- **Repos:** dizhaky/hermes-agent
- **Done:** Follow-up to merged #206 leftover Codex P2s. `resolve_litellm_target` now checks v12 `providers[litellm_proxy]` (key or name) before the compatibility list so a stale legacy `custom_providers` URL cannot win. `_needs_safety_gate_resume` matches the distinctive `blocked for safety:` prefix from `_guard_job_credential_exfil` instead of generic `not allowed` substrings.
- **Follow-up:** Do not merge until review; live apply of the one-shot script on mfc1 remains an operator step.

## 2026-08-25T22:30:00Z — Remote-triggered gateway restart via autodeploy (Claude Code)

- **Agent/tool:** Claude Code (remote session)
- **Repos:** dizhaky/hermes-agent
- **Context:** Dan requested "Restart Hermes." The mfc1 gateway was posting cron output but not answering interactive messages (Dan's fix-now request in #open-claude-health 21:18 UTC went unanswered, as did a direct restart instruction in #bridge). The remote session has no SSH path to mfc1, so this commit moves `main` to trigger `hermes-agent-autodeploy.timer` (the unit mfc1 actually runs is installed from `hermes-infrastructure/configs/systemd/user/`, which polls every 5 min + ≤60s jitter — not this repo's generic `deploy/` example, which polls every 15 min), which pulls and runs `systemctl --user restart hermes-gateway.service` — the designed out-of-band restart lever.
- **Done:** This system-log entry is the deploy-bump; no code changes.
- **Follow-up:** Confirm gateway active post-restart; Dan's four open items from #open-claude-health remain (mfc1 Tailscale down, config integrity re-seal, vault-sync push, Air disk 93%).
