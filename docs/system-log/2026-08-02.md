# 2026-08-02

## 2026-08-02T19:05:00Z — PR #137 salvaged onto a clean branch; npm-pin revert dropped (Claude Code, remote session)

- **Agent/tool:** Claude Code (remote session, branch `claude/mindbase-evaluation-opdwue`)
- **Repos:** hermes-agent
- **Done:** Re-landed the reviewed-good content of **#137** ("Secrets hardening and
  persistent hosting support for the gateway") on a branch cut from current `main`,
  and closed #137. #137 could not merge: `mergeable_state: dirty`, and it sat on
  `claude/slack-session-94aae0` — the branch of closed #106, which #130 said should
  not be continued.

  Carried forward (9 files, byte-identical to #137): `tools/env_passthrough.py`
  (CodeQL clear-text-logging fixes — the refusal path logged the caller-supplied
  variable name, now counts and static strings; the config-read handler logs the
  exception type rather than `str(e)`, since a YAML parse error quotes the offending
  line); and the 24/7 gateway hosting story — `deploy/hermes-gateway.service`,
  `deploy/config.example.yaml`, `deploy/gateway.env.example`, `docs/DEPLOYMENT.md`,
  `website/docs/guides/persistent-hosting.md`, plus `Dockerfile` (adds the
  `onepassword` extra), `docker-compose.yml` and one `README.md` row.

  **Dropped:** `.claude/settings.json`, which reverted
  `@modelcontextprotocol/server-memory` from `0.6.2` back to `0.6.3`. That version
  does not exist on npm — the published line jumps `0.6.2` to `2025.4.25` — so the
  revert re-breaks the `memory` MCP server and undoes #134. It was also the *only*
  merge conflict with `main`, so dropping the defect and clearing the conflict were
  the same edit. `hermes_cli/config.py` was not in #137's diff, so the
  GHSA-mv8x-fg99-32mf `_sanitize_env_lines` regression was not in play.

  **Added beyond #137:** registered the new guide in `website/sidebars.ts` (a manual
  list, so the guide's `sidebar_position` frontmatter is inert) — without it the page
  shipped orphaned from navigation while `README.md` and `docs/DEPLOYMENT.md` both
  linked its published URL, and `onBrokenLinks: 'warn'` meant nothing failed. Also
  added five regression tests pinning that registration never writes a variable name
  to the log; confirmed three of them fail against `main`'s version of the module.

  **Verified:** 26 tests pass in `tests/tools/test_env_passthrough.py` +
  `test_skill_env_passthrough.py` (both repo conftests loaded); `ruff` clean;
  `systemd-analyze verify` clean on the unit; sample config keys all read by
  `agent/secret_sources/onepassword.py`; every CLI invocation quoted in the new docs
  confirmed against `main`'s argparse; all relative doc links resolve. Only
  placeholder credentials appear anywhere (`ops_...your-token...`).

  **Not verified here** (no daemon in this container): the Docker image build and the
  systemd unit's runtime behaviour. The Docusaurus build was not run —
  `website/node_modules` is absent — so the sidebar was validated by parsing the file
  and resolving every `guides/*` id instead.

- **Commits/PRs:** `547666c`, `678110e`, `ca7f1b1`; supersedes #137
- **Follow-up:** Four guides were *already* orphaned from `website/sidebars.ts` on
  `main` — `google-gemini`, `local-ollama-setup`, `minimax-oauth`,
  `pipe-script-output`. Same failure mode, pre-existing and unrelated; left for a
  separate docs pass rather than widening a salvage PR.

## 2026-08-02T19:30:00Z — Four orphaned guides registered in the docs sidebar (Claude Code, remote session)

- **Agent/tool:** Claude Code (remote session, branch `claude/mindbase-evaluation-opdwue`)
- **Repos:** hermes-agent
- **Done:** Closed out the follow-up recorded in the 19:05 entry above. `website/sidebars.ts`
  enumerates the Guides category by hand, and four pages had never been added to it:
  `google-gemini`, `local-ollama-setup`, `minimax-oauth`, `pipe-script-output`. Each built
  and resolved by direct URL but appeared nowhere in site navigation.
  `docusaurus.config.ts` sets `onBrokenLinks: 'warn'`, so this never failed a build — which
  is why four of them accumulated unnoticed.

  Placed by subject rather than appended: `local-ollama-setup` beside `local-llm-on-mac`
  (both local-model guides), `pipe-script-output` beside `cron-script-only` (both scripted
  automation), and `google-gemini` / `minimax-oauth` inside the existing provider block with
  `aws-bedrock` / `azure-foundry` / `xai-grok-oauth`.

  **Verified:** `website/docs/guides/` holds 29 `.md` files and the sidebar now lists 29
  unique guide ids — a 1:1 match, so there are no orphans left and no duplicates. Every id
  resolves to a real file, and the file still parses under Node 22. The full Docusaurus build
  was not run (`website/node_modules` absent in this container), so this is a parse +
  id-resolution check rather than a rendered-site check.

- **Commits/PRs:** #139
- **Follow-up:** none. The orphan check is a one-liner over `website/docs/guides/` vs
  `sidebars.ts` and would be worth folding into `docs-site-checks` so the next hand-added
  guide fails loudly instead of silently — not done here.
